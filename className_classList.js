// first.className = "text-black red";   // to edit the class name
// first.classList.add("red");   // to add the class
// first.classList.remove("red");   // to remove the class
// first.classList.contains("red");   // to check if it is presents
// first.classList.toggle("red");   // adds the class if it doesn't exists, otherwise removes it
